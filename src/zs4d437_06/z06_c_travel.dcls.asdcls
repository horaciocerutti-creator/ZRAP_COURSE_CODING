@EndUserText.label: 'Acc Ctrl for projection layer'
@MappingRole: true
define role Z06_C_TRAVEL {
    grant
        select
            on
                Z06_C_TRAVEL
                    where
                        inheriting conditions from entity Z06_R_TRAVEL;
                        
}